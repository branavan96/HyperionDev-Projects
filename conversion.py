'''
You are given three sets of numbers '99.23', '23', '150' and a string "100".
'''

'''
We convert num1 into an integer by using the function 'int'.
To display output, we insert int_num1 into the print function.
To classify the output, we use the 'type' function and put 'int_num1' inside and we print it out.
'''

# the first input
num1=99.23 

# conversion of first input into an integer
int_num1=int(num1) 

# the output of the first conversion
print(int_num1)

# the classification of the first conversion
print(type(int_num1)) 

'''
We convert num2 into a float by using the function 'float'.
To display output, we insert float_num2 into the print function.
To classify the output, we use the 'type' function and put 'float_num2' inside and we print it out.
'''

# the second input
num2=23 

# conversion of second input into a float
float_num2=float(num2) 

# the output of the second conversion
print(float_num2)

# the classification of the second conversion
print(type(float_num2))

'''
We convert num3 into a string by using the function 'str'.
To display output, we insert str_num3 into the print function.
To classify the output, we use the 'type' function and put 'str_num3' inside and we print it out.
'''

# the third input
num3=150 

# conversion of third input into a string
str_num3=str(num3) 

# the output of the third conversion
print(str_num3)

# the classification of the third conversion
print(type(str_num3)) 

'''
We convert string1 into an integer by using the function 'int'.
To display output, we insert int_string1 into the print function.
To classify the output, we use the 'type' function and put 'int_string1' inside and we print it out.
'''

# the fourth input
string1="100" 

# conversion of fourth input into an integer
int_string1=int(string1) 

# the output of the fourth conversion
print(int_string1) 

# the classification of the fourth conversion
print(type(int_string1)) 